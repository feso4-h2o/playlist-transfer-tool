# Replay Search Fixtures

This directory contains historical Spotify and QQ Music search-result fixtures
used by offline regression tests.

These fixtures are not proof of current live API behavior. Refresh them only
with credential-free, metadata-only captures, and keep credentials, cookies,
private IDs, sensitive headers, audio, lyrics, cover art, databases, reports,
caches, and local configuration out of tracked data.
